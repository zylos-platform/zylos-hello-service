# application-local.yml

management:
  endpoints:
    web:
      exposure:
        include: health,info
  tracing:
    export:
      otlp:
        enabled: false
      enabled: false
  otlp:
    metrics:
      export:
        enabled: false
